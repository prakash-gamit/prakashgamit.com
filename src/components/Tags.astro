---
const allPosts = Object.values(import.meta.glob('../pages/posts/*.md', { eager: true }));
const tags = [...new Set(allPosts.map((post: any) => post.frontmatter.tags).flat())];
---
<ul>
    {tags.map((tag: any) => <li><a href={`/tags/${tag}`}>{tag}</a></li>)}
</ul>